# Which DriverFactory branch to use: chrome | firefox
browser=chrome

# Run without a visible browser window (set to false to watch it locally)
headless=true

# Target application under test - swap this for your own project's URL
base.url=https://www.saucedemo.com/

# Seconds WebDriver waits implicitly for elements before failing
implicit.wait=10
